version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.development
    ports:
      - "3000:3000"
    volumes:
      - .:/rails
      - storage:/rails/storage
    environment:
      - RAILS_MASTER_KEY=1104f8eadc36b69f81acb5468a3a7030
volumes:
  storage:
